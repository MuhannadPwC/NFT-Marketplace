import { Button, Container, Heading, useColorMode } from "@chakra-ui/react";

const Home = () => {

  return (
    <Container>
      <Heading color="Blue">Home</Heading>
    </Container>
  );
};

export default Home;
